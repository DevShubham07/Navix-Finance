// Borrower login entry point.
// TODO: render login form (mobile/PAN), call backend IAM auth, persist session.

export default function LoginPage() {
  return (
    <section>
      <h1>Log in to NAVIX Finance</h1>
      <p>Sign in to continue your application or manage your loan.</p>
    </section>
  );
}
