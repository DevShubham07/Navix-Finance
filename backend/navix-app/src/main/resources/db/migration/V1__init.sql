-- Flyway baseline migration for NAVIX Finance.
--
-- This is an intentional placeholder so Flyway has a starting point.
-- TODO: real DDL TBD — tables for IAM/staff, borrowers, onboarding, KYC,
--       verification, income & risk assessment, loans, disbursement, and
--       collections will be added in subsequent versioned migrations
--       (V2__*.sql, V3__*.sql, ...).

-- No-op statement so this migration is valid even before real schema exists.
SELECT 1;
