package com.navix.loan.domain;

/** Which side of a referral a payout rewards: the person who shared the code, or the new borrower. */
public enum ReferralBeneficiaryRole {
    REFERRER,
    REFERRED
}
